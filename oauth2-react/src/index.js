import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App"; // App 안에서 Router 설정되어 있음

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
